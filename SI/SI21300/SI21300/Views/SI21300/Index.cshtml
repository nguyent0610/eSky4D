@using Ext.Net;
@using Ext.Net.MVC;
@using eBiz4DWebFrame;
@using eBiz4DWebFrame.HQControl;
@using SI21300;

@model System.Collections.IEnumerable
@{
    Layout = null;
    var X = Html.X();
    var HQ = Html.HQ();
    ViewBag.Title = "SI21300";
    var acc = Session["SI21300"] as AccessRight;
}
<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width" />
<script src="~/Scripts/common.js"></script>
<script src='@Url.Content("Scripts/Screen/SI21300.js")' type="text/javascript"></script>
<style> @*Thuoc tinh hover tren grid *@
    .x-grid-row-over .x-grid-cell-inner {
        font-weight : bold;
    }
  
  
</style>
<link href="~/Content/ComboXTemplate.css" rel="stylesheet" />
<script>
    var isUpdate = strToBool('@acc.Update.ToString().ToLower()');
    var isInsert = strToBool('@acc.Insert.ToString().ToLower()');
    var isDelete = strToBool('@acc.Delete.ToString().ToLower()');
</script>
<script>
    //check value
    var isAllValid = function (items) {
        if (items != undefined) {
            for (var i = 0; i < items.length; i++) {

                
                if (items[i]["CarrierID"] == undefined) continue;
            
                if (items[i]["CarrierID"].trim() == "") {
                callMessage(15, '@Util.GetLang("CarrierID")', null);
                    return false;


                }
                if (items[i]["CarrierType"].trim() == "") {
                    callMessage(15, '@Util.GetLang("CarrierType")', null);
                    return false;
                }
                if (items[i]["TerritoryID"].trim() == "") {
                    callMessage(15, '@Util.GetLang("TerritoryID")', null);
                    return false;
                }
                if (items[i]["ShipAccount"].trim() == "") {
                    callMessage(15, '@Util.GetLang("ShipAccount")', null);
                    return false;
                }
                if (items[i]["UOM"].trim() == "") {
                    callMessage(15, '@Util.GetLang("UOM")', null);
                    return false;
                }

           
            



        }
        return true;
    } else {
        return true;
    }
    };

</script>
@*Script chung*@    
  <title>@ViewBag.Title</title>
    </head>
    <body>

@(Html.X().ResourceManager().Theme((Theme)Enum.Parse(typeof(Theme),Current.Theme.PassNull()==""?"Gray":Current.Theme.PassNull())))

     @{ Html.RenderAction("Body"); }
    </body>
</html>
 