@using Ext.Net;
@using Ext.Net.MVC;
@using eBiz4DWebFrame;
@using eBiz4DWebFrame.HQControl;
@using SI21700;

@model System.Collections.IEnumerable
@{
    Layout = null;
    var X = Html.X();
    var HQ = Html.HQ();
    ViewBag.Title = "SI21700";
    var acc = Session["SI21700"] as AccessRight;
}
<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width" />
<link href="~/Content/ComboXTemplate.css" rel="stylesheet" />
<script src="~/Scripts/common.js"></script>
<script src='@Url.Content("Scripts/Screen/SI21700.js")' type="text/javascript"></script>
<style> @*Thuoc tinh hover tren grid *@
    .x-grid-row-over .x-grid-cell-inner {
        font-weight : bold;
    }
  
  
</style>

<script>
    var isUpdate = strToBool('@acc.Update.ToString().ToLower()');
    var isInsert = strToBool('@acc.Insert.ToString().ToLower()');
    var isDelete = strToBool('@acc.Delete.ToString().ToLower()');
</script>
<script>
    //check value
    var isAllValid = function (items) {
        if (items != undefined) {
            for (var i = 0; i < items.length; i++) {

                
                if (items[i]["Country"] == undefined && items[i]["State"] == undefined && items[i]["District"] == undefined) continue;
                if (items[i]["Country"] == "" && items[i]["State"] == "" && items[i]["District"] == "") continue;
            
                if (items[i]["Country"].trim() == "") {
                callMessage(15, '@Util.GetLang("Country")', null);
                    return false;


                }
                if (items[i]["State"].trim() == "") {
                    callMessage(15, '@Util.GetLang("State")', null);
                    return false;
                }
                if (items[i]["District"].trim() == "") {
                    callMessage(15, '@Util.GetLang("District")', null);
                    return false;
                }
                if (items[i]["Name"].trim() == "") {
                    callMessage(15, '@Util.GetLang("Name")', null);
                    return false;
                }
               
           
            



        }
        return true;
    } else {
        return true;
    }
    };

</script>
@*Script chung*@    
  <title>@ViewBag.Title</title>
    </head>
    <body>

@(Html.X().ResourceManager().Theme((Theme)Enum.Parse(typeof(Theme),Current.Theme.PassNull()==""?"Gray":Current.Theme.PassNull())))

     @{ Html.RenderAction("Body"); }
    </body>
</html>
 